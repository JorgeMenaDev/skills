# v5.2.1 Release Run — upgrade-recap friction fixes (2026-07-15)

Same-day follow-up to v5.2.0: doc-contract fixes for the seven friction findings from the first live upgrade-recap run ([skills#137](https://github.com/JorgeMenaDev/skills/issues/137), pilot: acredix.cl, [matias@234e498](https://github.com/JorgeMenaDev/matias/commit/234e498)). Canonical machine record: `gate-results-5.2.1.json`.

## Fixes (finding → change)

| # | Finding | Fix |
| --- | --- | --- |
| 1 | No defined home for the recap report | Contract + template both fix `reports/YYYY-MM-DD-upgrade-recap-<installed version>.md` (version-suffixed after autoreview flagged same-day recap-after-recap collisions — acredix.cl would have overwritten today's 5.2.0 report); the stamp's `report` field records that path |
| 2 | "Marked stale" had no mechanism | Named additive `staleAsOf`/`staleReason` ledger annotation + the backlog row as the operative exit; § Coverage certification now rules an unresolved stale annotation as stale regardless of `observedAt` (session-honored; the reader stays unchanged) |
| 3 | Recap-as-ticket ambiguity | Direct operator invocation creates no retroactive ticket — report + log + stamp are the canonical audit trail; an existing recap ticket closes against the report |
| 4 | Re-triage balloons when gates unchanged | Check 2 scoped to the version window; unchanged gate-owners get a light conformance scan; outcomes labeled `version-driven` / `incidental hygiene`; incidental fixes only when cheap-and-obvious |
| 5 | Invalidation granularity undefined | Load-bearing test: invalidate only when the changed clause alters what the rung's method would observe, accept, or reject; per-rung reasoning recorded either way |
| 6 | No upgrade-context field | Template gains an "Upgrade context" section (drift state found + load-bearing semantic diff); contract requires recording it before the checks |
| 7 | Operator identity for agent-executed runs | Header convention `authorized-by / executed-by` |

Files: `references/never-dry-loop.md` (recap pass + coverage certification), `templates/upgrade-recap.md`, `SKILL.md` 5.2.0 → 5.2.1, `scripts/bootstrap-seo-workspace.mjs` `SKILL_VERSION`, `evaluate-release.mjs` `GATE_RESULTS_FILENAME`. No script behavior changes; no ratified recap decision reopened.

## Harness state at release

| Command | Exit | Result |
| --- | --- | --- |
| `node dev/seo-growth-workspace/validate-skill.mjs` | 0 | PASS — 52/52 sections |
| `node dev/seo-growth-workspace/command-inventory.mjs --verify` | 0 | PASS — 0 malformed |
| `node dev/seo-growth-workspace/evaluate-release.mjs --json` | 0 | `pass: true`, 100/100, rejections none |

Manual gates: no owning specialist reference is altered (b01/b02/b07/b08/b16 owners byte-unchanged from v5.2.0); all seventeen re-attested.

## Propagation note

On refresh, the acredix-landing workspace (reconciled to 5.2.0 today) drifts again by design — its recap under 5.2.1 is the first live test of the recap-after-recap path and should be near-empty (the load-bearing diff is the recap contract itself). The other six workspaces reconcile straight to 5.2.1.
