# v5.2.3 Release Run — third-order recap friction fixes (2026-07-15)

Third same-day follow-up: fixes for the twelve deduped findings from the first two recaps run under the v5.2.2 contract ([skills#141](https://github.com/JorgeMenaDev/skills/issues/141); runs: superaseo never-baseline at 52 rows with successful window reconstruction [matias@7a6a3b9](https://github.com/JorgeMenaDev/matias/commit/7a6a3b9), acredix-landing 5.2.0→5.2.2 recap-after-recap [matias@bb65001](https://github.com/JorgeMenaDev/matias/commit/bb65001)). The v5.2.2 core held: window reconstruction, the degenerate load-bearing test, and the upgrade-context section all did their jobs; these are the residuals. Canonical machine record: `gate-results-5.2.3.json`.

## Fixes (finding → change)

| # | Finding | Fix |
| --- | --- | --- |
| 1 | Pre-commit pull-rebase fails on the run's own dirty tree (contract contradiction) | Recap pass: the pre-commit pull-rebase runs with autostash over the run's own uncommitted writes — safe precisely because no sibling shares the tree |
| 2 | Amendment license ambiguous: keyed on class or cost? | Check 2: the cheap-and-obvious license keys on cost, never on class; class records attribution, not permission; a filed row whose only content is a one-line amendment of another open row is the wrong exit |
| 3 | Check-1 dispositions can't express findings owned elsewhere | Fourth disposition **noted (owned elsewhere)**, naming the owner — not repaired, not deleted, never filed as site work; template column updated |
| 4 | Check-4 scope (ledger rows only vs demanded-but-missing companions) | Bidirectional: rows conform AND obligated ship history has companions; missing companions filed as normal rows, never fabricated retroactively |
| 5 | Skipped rows neither open nor closed for re-triage | Dated skip dispositions are closed for re-triage purposes |
| 6 | Zero-delta recap vs backlog header freshness | A recap changing no backlog row leaves the backlog file untouched entirely, headers included — report/log/stamp are the record |
| 7 | Old stamps' `report` values vs current naming rule | Grandfathered: a stamp's `report` conforms to the naming rule in force when written, never re-validated against later rules |
| 8 | `basis` canonicalization invites rename-hunts when the field is absent | The canonical key governs values being written; an absent basis is never fabricated or renamed into existence |
| 9 | Recap-after-recap light-scan corollary | A same-day prior recap's full re-judgment under byte-identical gates satisfies the light scan |
| 10 | Window evidence is archaeology | Installed-copy diffing across the consuming repo's install commits blessed as the canonical window-evidence method for every baseline; optional additive `createdUnderSkillVersion` loop-file field + running-version-in-report-header convention make future reconstruction mechanical |
| 11 | Reconciliation stamp invisible to tooling (#139 finding 10, now evidence-backed twice) | `cadence-status.mjs` JSON report carries an additive advisory `reconciliation` block (`stampState` present/absent/malformed + stamp fields). Deliberately version-independent: the script never reads the installed version, so expected fixtures stay stable across releases and drift semantics stay owned by the contract. Fixture set extended (`reconciled-stamp`, `malformed-stamp`); 11 existing JSON fixtures regenerated (diff = the additive block only) |
| 12 | Recaps keep paying for state-creation gaps | Same-pass hooks: a Done row for shipped work references its ship event or records non-qualification (§ Ship-rate ceiling); a dated recheck promise in a Done row/log entry IS an obligation — ledger or recorded exemption (§ Measurement obligations) |

Files: `references/never-dry-loop.md`, `templates/upgrade-recap.md`, `scripts/cadence-status.mjs` (+ usage text), `dev/.../validate-skill.mjs` (2 new fixture cases), `dev/.../fixtures/cadence-status/` (2 new fixtures, 11 regenerated), `SKILL.md` 5.2.2 → 5.2.3, `scripts/bootstrap-seo-workspace.mjs` `SKILL_VERSION`, `evaluate-release.mjs` `GATE_RESULTS_FILENAME`. One deliberate script behavior change (the advisory reconciliation block); no ratified decision reopened.

## Harness state at release

| Command | Exit | Result |
| --- | --- | --- |
| `node dev/seo-growth-workspace/validate-skill.mjs` | 0 | PASS — 52/52 sections (cadence-status fixture cases now 19) |
| `node dev/seo-growth-workspace/command-inventory.mjs --verify` | 0 | PASS — 0 malformed |
| `node dev/seo-growth-workspace/evaluate-release.mjs --json` | 0 | `pass: true`, 100/100, rejections none |

Manual gates: no manual gate's owning specialist reference is touched; all seventeen re-attested with a dated note.

## Propagation note

Not propagated to the Matias hub yet, deliberately: superaseo and acredix-landing were reconciled to 5.2.2 today, and immediate propagation would re-drift them. Recommended sequencing: propagate 5.2.3 when the remaining five workspaces' recaps are scheduled, so the fleet reconciles once to 5.2.3 instead of twice. The stale-annotation fresh-write path remains field-untested.
