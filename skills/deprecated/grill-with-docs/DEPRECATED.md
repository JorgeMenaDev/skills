# DEPRECATED — 2026-07-08

This fork's rich body (interview + docs discipline + target-repo routing in one skill) is retired. Do not install it.

- **Target-repo routing** now lives in the owned fork [`skills/software-development/domain-modeling`](../../software-development/domain-modeling/) — so wayfinder, triage, and every other direct `/domain-modeling` invocation gets it, not just the wrapper.
- **`grill-with-docs`** is consumed from upstream `mattpocock/skills` (a one-line composition of `/grilling` + `/domain-modeling`).
- Decision: two-vendor counsel, 3 rounds, converged (matias profile session, 2026-07-08).

Kept for history only.
