# Product Context

- Product:
- One-line value proposition:
- ICP:
- PostHog project id / URL:
- Canonical site origin + allowed entry hosts (for site-scoped organic outcomes):
- North-star metric:
- Primary conversion event(s), snake_case (one outcome may have multiple terminal paths):
- Organic outcome mapping (optional; event names only when they exist in PostHog and carry the original organic session ID):
  - Same-session qualified lead event:
  - Same-session customer event:
- Activation definition (event + why it means value was delivered):
- Funnel stages (3–6 existing events, first touch → activated):
  1.
- Constraints (market, language, consent tier, traffic scale):
