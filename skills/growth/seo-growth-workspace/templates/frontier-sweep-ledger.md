# Frontier sweep — YYYY-MM-DD

## Cadence state

- Target:
- Sweep cadence ID / due window:
- Cursor before / after:
- Coverage max-age policy:
- Cooldown policy:
- Candidate cap / ship-rate ceiling:
- Engine `autoPublish` / gate state (`armed | gated | disarmed | unknown`):
- Engine state checked at / evidence:
- Engine schedule `enabled`:
- Engine schedule `daysOfWeek`:
- Engine schedule `hourLocal` / timezone:
- Next publish window:
- Live quality-watch occurrence / ticket:

## Invocation fingerprint

Copy the canonical fingerprint values from `references/never-dry-loop.md`; do not add or reinterpret fields.

| target | mode | requestedSurface | remit | mutationCeiling | authorizationClass |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## Traversal

| Rung | Due reason | Source / checked at | Coverage artifact | Candidate fingerprint | Gate result / failing gate | Owner + recheck or closure |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

## Result

- Terminal:
- Eligible candidate / reused or created ticket:
- Coverage (`complete | partial`):
- Top three rejected candidates + failing gates (required for `nothing valuable this cycle`):
- Earliest next due:
- Observable wake predicates:
- Evidence limitations / blocker:
