REPLACE — the implement agent's repo orientation, injected as the `# CONTEXT`
section body: canonical docs to read first, monorepo/app shape, and the repo
rules that bind the agent (copy style, no-test policy, deploy targeting, ...).
