# BOOT THE APP

REPLACE — exact commands to build/serve the app for verification, including a
readiness poll, plus an `# AUTHENTICATE` section if routes are auth-gated
(QA actor flow — never a personal account). Add a `# SCOPE` section if only
part of the app is verifiable.
