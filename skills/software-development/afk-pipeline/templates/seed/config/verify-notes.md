# REPO QA NOTES (learned by earlier sessions — don't rediscover)

- REPLACE: repo-specific QA gotchas (auth gates, slow async jobs, shared dev
  deployments, test-mode payment cards, tooling quirks).
