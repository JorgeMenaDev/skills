interface:
  display_name: "Andes release"
  short_description: "Tag and publish GitHub Releases that match the stores and production"
  default_prompt: "Use $andes-release to cut the release for this build"
